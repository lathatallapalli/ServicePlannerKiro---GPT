declare module '@carbon/icons/es/*/32' {
  const icon: {
    elem: string;
    attrs: Record<string, string | number | boolean>;
    content: Array<{
      elem: string;
      attrs: Record<string, string | number | boolean>;
      content?: unknown[];
    }>;
    name: string;
    size: number;
  };

  export default icon;
}
